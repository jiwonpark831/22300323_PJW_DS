#include <iostream>
using namespace std;
#include <algorithm>
#include <vector>

int main(){
    int num;
    int k;
    cin >> num >> k;
    vector<int> v;
    for(int i = 0 ; i < num ; i++){
        int input;
        cin >> input;
        v.push_back(input);
    }









    return 0;
}
